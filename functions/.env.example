# Deployment-time, non-secret Functions settings.
VENDORA_FUNCTION_REGION=us-central1

# Keep false during the App Check metrics/compatibility rollout. Set true only
# after the web app is registered, the client site key is deployed, and valid
# request metrics have been verified. The emulator always bypasses enforcement.
CHECKOUT_ENFORCE_APP_CHECK=false
